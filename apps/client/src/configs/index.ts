// /* eslint-disable unicorn/prefer-module */
// import moduleAlias from 'module-alias';
// import path from 'node:path';

// moduleAlias.addAlias('@server-trpc', path.resolve(__dirname, '../../server/src/trpc"'));
// moduleAlias.addAlias('@client-trpc', path.resolve(__dirname, './src/trpc/trpc.ts'));

// moduleAlias();
export {};
