// import { createUser } from '../interfaces/authorization-interfaces/user-interface/user-interface';

export const loginRequest = async () => true;
// export const loginRequest = async (username = '', password = '') => true;

// export const registerRequest = async (data: createUser) => true;
